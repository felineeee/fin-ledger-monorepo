# Lifecycle

- Get payment config, getting `id` including internal `metadata` from `payment-methods`
-

## Gateway Checkout

- Create `payment-methods`, then create a `payments`. This branching in `IN_PERSON` or `ONLINE`

### ONLINE

- Get the `gateway` create checkout. After that we need mechanism on getting the webhook if its paid, through `webhooks`, then we can see the `checkout-session` from `gateway`

### IN_PERSON

- Get the `capture` to `capture-cash` then see the `payments` if it succeeded

# Todo

- Separating `gateway-config` and `gateway`
- Make clear differences on `checkout` and `gateway`

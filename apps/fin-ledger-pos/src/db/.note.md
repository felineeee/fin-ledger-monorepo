# TODO

* Choices here is to bootstrap app and uses the exported `DB` or `PG_POOL`, or open a raw connection
* Should add seeder config of const, on configuring how many each entries
* So migrations are meant to be immutable once applied. So editing migration file once its migrated wont apply anything.
